# Admin route group placeholder

This route group is intentionally non-functional. ADR-003 uses GitHub/MDX authoring, so the current MVP must not expose `/admin`, auth, CRUD, preview, media library, or Tiptap routes.

If a browser-based CMS becomes necessary later, write a new ADR before adding any admin route, Supabase/Auth dependency, or write API.
